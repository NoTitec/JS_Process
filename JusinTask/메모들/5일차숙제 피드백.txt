숙제 피드백

숫자대려박는 것보단 상수화시키기
반복문 제어 bool변수로 만들어서 제어하기

콘솔창 제어 문장
system("cls") 콘솔창 내용 지우기
system("pause") 키 입력까지 프로그램 일시정지

랜덤수 만들기

1~10 범위 수라면

time 함수 사용위한 헤더파일 ctime
srand(unsigned(time(NULL))

int i= rand() %10 +1 을 하면됨

