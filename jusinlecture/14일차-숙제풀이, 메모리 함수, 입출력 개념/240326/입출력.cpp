#include <iostream>

using namespace std;

int main()
{
	// 스트림 : 각종 하드웨어 장치와 프로그램을 연결해주는 일종의 가상의 통로(단 방향성)
	// 버퍼 : 데이터를 임시 저장하기 위한 메모리 블럭
	
	// 콘솔 입출력의 스트림

	// stdin  : 표준 입력 스트림(기본 키보드 대상)
	// stdout : 표준 출력 스트림(기본 모니터 대상)
	// stderr : 표준 에러 스트림(기본 모니터 대상)

	// 텍스트 모드 : 문자열과 관련된 형태로 제공되는 상태
	// 바이너리 모드 : 정수, 구조체와 같이 문자열이 아닌 다른 형태로 제공되는 상태
		
	return 0;
}